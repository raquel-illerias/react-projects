import styled from "styled-components";
import { Box } from "@mui/system";

export const KcalContainer = styled(Box)`
    &.MuiBox-root{
        margin-left: 1rem;
        color: #656870;
    }`
    
